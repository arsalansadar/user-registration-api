spring:
  application:
    name: userservice

  datasource:
    url: jdbc:mysql://localhost:3306/userDB?createDatabaseIfNotExist=true
    username: root
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: ${JWT_SECRET}
